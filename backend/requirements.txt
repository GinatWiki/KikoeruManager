fastapi>=0.104.0
uvicorn[standard]>=0.24.0
sqlalchemy>=2.0.0
psycopg[binary]>=3.2
alembic>=1.14
pydantic>=2.5.0
pydantic-settings>=2.1.0
pyyaml>=6.0.1
watchdog>=3.0.0
filetype>=1.2.0
requests>=2.31.0
# httpx 在响应 Content-Encoding=br 时需要 brotli/brotlicffi 解压；
# 用 brotlicffi 是因为它纯 cffi 实现，对新 Python 版本（3.13/3.14+）兼容性
# 比 brotli（C 扩展）好得多。缺这个包 DLsite 返回的 br 压缩页面会被
# httpx 当作原始字节，response.text 就是乱码，社团补全 / 关键字搜索全线挂掉。
brotlicffi>=1.1.0
httpx>=0.25.0
socksio>=1.0.0
litellm>=1.60.0
pikpakapi==0.1.11
transferit-py>=0.1.0rc1
# 邮件模板块渲染后的 HTML 白名单清洗；缺失时会回退正则清洗，但邮件安全边界会变弱。
nh3>=0.2.18
python-multipart>=0.0.6
aiofiles>=23.2.1
websockets>=12.0
apscheduler>=3.10.0
aiohttp>=3.9.0
mutagen>=1.47.0
opencc-python-reimplemented==0.1.7
croniter>=1.3.0
# orjson：给 SA engine 的 json_serializer / json_deserializer 用，替换 stdlib json 后
# ActivityLog.detail 等 JSON 列的反序列化从 ~110ms/762 行降到 ~20ms，端到端 list 接口
# 首屏延迟显著下降。
orjson>=3.9.0
pystray>=0.19.5
Pillow>=10.1.0
pyinstaller>=5.0.0
imapclient>=3.0.1
qrcode[pil]>=7.4.2
redis>=5.0.0
